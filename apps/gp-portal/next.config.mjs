/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ['@referralplatform/ui-components', '@referralplatform/shared-types'],
};

export default nextConfig;

{
  "name": "@referralplatform/audit-client",
  "version": "0.1.0",
  "description": "TS client every service imports to write to, and query, the Audit Log Service's API. See root CONVENTIONS.md ('Using packages/audit-client').",
  "private": true,
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@referralplatform/shared-types": "*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.16.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.12",
    "typescript": "^5.6.3"
  }
}

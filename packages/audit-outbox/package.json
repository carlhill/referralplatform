{
  "name": "@referralplatform/audit-outbox",
  "version": "0.1.0",
  "description": "The outbox pattern's shared implementation — enqueue and relay logic every service uses to deliver audit events durably. See root CONVENTIONS.md §7.",
  "private": true,
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@referralplatform/audit-client": "*",
    "@referralplatform/shared-types": "*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.16.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.12",
    "typescript": "^5.6.3"
  }
}
